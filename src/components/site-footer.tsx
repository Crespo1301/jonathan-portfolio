export function SiteFooter() {
  return (
    <footer className="border-t border-border bg-surface">
      <div className="shell flex flex-col gap-3 py-8 text-sm text-muted md:flex-row md:items-center md:justify-between">
        <p>Jonathan Portfolio is being prepared as a CSolutions client build, hosting and support are fully comped.</p>
        <p>Launch target: Vercel preview first, custom domain later.</p>
      </div>
    </footer>
  );
}
